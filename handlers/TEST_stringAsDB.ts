import { Interaction } from "discord.js";
import { createEphemeral } from "../utils/helpers";

// const TEST_dbMessageId  = '';

// export const handleStringAsDB = async (interaction: Interaction) => {
//   if (!interaction.isCommand()) return;

//   const zoomLink =
//     interaction.options.get("zoom-link")?.value;
//   const message = interaction.options.get("message")?.value;

//   const newMessage = `@everyone ${message} ${zoomLink}`;

//   await interaction.reply(newMessage);
//   return;
// };
